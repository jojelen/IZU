# I Zee yoU

This is a sandbox repo for playing around with tensorflow lite.

## Dependencies

* OpenCV for webcam video.

* Flatbuffers
    -> git clone  https://github.com/google/flatbuffers.git &&
    cd flatbuffers && cmake CMakeLists.txt && make && make install
    && ldconfig

* Tensorflow lite
    -> install by running
        make -f tensorflow/lite/tools/make/Makefile TARGET=linux
    This creates a static lib that one has to link to.
